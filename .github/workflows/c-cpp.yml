name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-focal
    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make test
